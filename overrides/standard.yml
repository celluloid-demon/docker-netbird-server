services:
  dashboard:
    image: netbirdio/dashboard:${NB_DASHBOARD_TAG}

  netbird-server:
    image: netbirdio/netbird-server:${NB_SERVER_TAG}
    volumes:
      - ${VOLUMES}/data:/var/lib/netbird
